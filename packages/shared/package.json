{
  "name": "@shulkr/shared",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --fix src/ && prettier --check --write src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@ts-rest/core": "^3.52.1",
    "zod": "^3"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^2"
  }
}
